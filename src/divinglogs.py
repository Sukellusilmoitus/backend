def create_new_diving_log(diver_id, wreck_id, location_is_correct,
                            x_coordinate, y_coordinate, new_coordinate_text,
                            changes_on_wreck, changes_info, created_at):

    return
